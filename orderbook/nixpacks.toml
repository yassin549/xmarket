# Nixpacks configuration for Orderbook service

[phases.setup]
nixPkgs = ["python312", "gcc"]

[phases.install]
cmds = ["cd .. && pip install -r requirements.txt"]

[start]
cmd = "cd .. && /app/.venv/bin/python -m uvicorn orderbook.main:app --host 0.0.0.0 --port $PORT"


