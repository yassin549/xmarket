# Nixpacks configuration for Reality Engine
# This service runs as a standalone Python application

[phases.setup]
nixPkgs = ["python312", "gcc"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "/app/.venv/bin/python run.py --backend-url $BACKEND_URL --verbose"

