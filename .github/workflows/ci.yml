name: CI

on:
  push:
    branches: [ "main", "dev" ]
  pull_request:
    branches: [ "main", "dev" ]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
    
    - name: Placeholder - Install dependencies
      run: echo "No package.json yet - CI scaffold only"
    
    - name: Placeholder - Run linter
      run: echo "Linting not configured yet"
    
    - name: Placeholder - Run tests
      run: echo "No tests yet - this is Phase 0 scaffold"
    
  agent-policy-check:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Check PR labels
      run: echo "TODO - Verify agent-proposal label for agent PRs"
    
    - name: Check for secrets
      run: echo "TODO - Scan diff for leaked secrets"
    
    - name: Verify safety assertions
      run: echo "TODO - Ensure agent PRs include safety_assertions"
